---
- name: Create Traffic Filter profiles and protections
  hosts: cc
  gather_facts: false
  vars_files:
    - ../vars/cc.yml
    - ../vars/create_vars.yml

  tasks:
    - name: Create and apply Traffic Filter configuration
      block:
        - name: Lock device(s)
          dp_lock:
            provider: "{{ cc }}"
            dp_ip: "{{ item }}"
          loop: "{{ dp_ip | default([]) }}"
          loop_control:
            label: "Locking device: {{ item }}"
          when: not (skip_device_lock | default(false))

        - name: Configure Traffic Filter profiles and protections
          create_traffic_filter:
            provider: "{{ cc }}"
            dp_ip: "{{ item }}"
            tf_profiles: "{{ create_tf_profiles | default([]) }}"
            tf_protections: "{{ create_tf_protections | default([]) }}"
          loop: "{{ dp_ip | default([]) }}"
          loop_control:
            label: "Device: {{ item }}"
          register: tf_create_results
          when: (create_tf_profiles is defined and create_tf_profiles | length > 0) or
                (create_tf_protections is defined and create_tf_protections | length > 0)

        - name: Display Traffic Filter creation results
          vars:
            response: "{{ item.response | default({}) }}"
            formatted_output: |
              **Device: {{ item.item }}**

              {% if response.preview_mode | default(false) %}
              PREVIEW MODE - Operations that would be performed:

              {% set planned_ops = response.planned_operations | default({}) %}

              {% if planned_ops.profiles | default([]) | length > 0 %}
              Traffic Filter Profiles to be created ({{ planned_ops.profiles | length }}):
              {% for p in planned_ops.profiles %}
                - {{ p.profile_name }}
              {% endfor %}
              {% else %}
              No profiles to be created.
              {% endif %}

              {% if planned_ops.protections | default([]) | length > 0 %}
              Traffic Filter Protections to be created ({{ planned_ops.protections | length }}):
              {% for t in planned_ops.protections %}
                - {{ t.protection_name }} (Profile: {{ t.profile_name }})
              {% endfor %}
              {% else %}
              No protections to be created.
              {% endif %}

              {% else %}
              EXECUTION RESULTS:

              **Traffic Filter Profiles created successfully:**
              {{ response.pretty_profiles | default('No profiles created') }}

              **Traffic Filter Protections created successfully:**
              {{ response.pretty_protections | default('No protections created') }}

              **Errors Detected:**
              {% if response.errors | default([]) | length > 0 %}
              - {{ response.errors | join('\n- ') }}
              {% else %}
              No errors detected.
              {% endif %}
              {% endif %}
          ansible.builtin.debug:
            msg: "{{ formatted_output.split('\n') }}"
          loop: "{{ tf_create_results.results | default([]) }}"
          loop_control:
            label: "Device: {{ item.item }}"

        - name: Apply Traffic Filter updates per device
          update_policies:
            provider: "{{ cc }}"
            dp_ip: "{{ item }}"
          loop: "{{ dp_ip | default([]) }}"
          loop_control:
            label: "Applying updates: {{ item }}"
          when:
            - not (skip_policy_updates | default(false))
            - tf_create_results is defined
            - tf_create_results.changed | default(false)

      always:
        - name: Unlock device(s)
          dp_unlock:
            provider: "{{ cc }}"
            dp_ip: "{{ item }}"
          loop: "{{ dp_ip | default([]) }}"
          loop_control:
            label: "Unlocking device: {{ item }}"
          failed_when: false
          when: not (skip_device_lock | default(false))
